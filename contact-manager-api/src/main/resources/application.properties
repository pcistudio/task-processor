# Application properties
# JPA
spring.datasource.url=jdbc:mariadb://localhost:3308/contactdb
spring.datasource.username=contact_manager
spring.datasource.password=contact123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# activate actuator endpoints
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

attachment.directory=attachments

# Application name
spring.application.name=contact-manager-api

# Logging
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.hibernate.SQL=DEBUG
## Current application log level
logging.level.com.contact.manager=DEBUG
debug=true

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dthought950@gmail.com
spring.mail.password=rybekzxbmejhxkfx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# TODO mail.blacklist=blacklist.txt


# Application
company.name=HostalHabana

# Flyway
spring.flyway.enabled=false

# CORS configuration
cors.allowed-origins=http://localhost:3000

# Cache
cache.persistence.directory=./cache
#to disable cache set
#spring.cache.type=none

spring.task.processor.writer.enabled=true
spring.task.handlers.enabled=true
spring.task.logging.template=true
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.com.pcistudio.task=TRACE

# Metrics
management.metrics.tags.application=truck-management
management.metrics.tags.environment=production
management.metrics.tags.region=us-east